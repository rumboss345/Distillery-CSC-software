# IBD knowledge for Ask Nelly

Plain-text extracts from the IBD Diploma in Distilling revision notes (Modules 1A, 1B, 2, syllabus).

Regenerate after updating source PDFs:

```bash
npm run extract:ibd-knowledge
```

Sources live in `sources/`. The Express server chunks them at startup for `/api/ask-nelly/*`.
